'use strict';
const fs=require('fs'),assert=require('assert');
const app=fs.readFileSync('app.js','utf8'),plans=fs.readFileSync('plans-v80.js','utf8'),authority=fs.readFileSync('arena-secure-authority-v1.js','utf8'),pwa=fs.readFileSync('pwa.js','utf8'),sw=fs.readFileSync('service-worker.js','utf8');
assert.match(app,/syncTrustedClock/);
assert.match(app,/Europe\/Istanbul/);
assert.match(plans,/authority\(\)\?\.verified/);
assert.match(plans,/allowLocalPlanSimulation/);
assert.match(authority,/ARENA-SERVER-AUTHORITY-V1/);
assert.equal((pwa.match(/serviceWorker\.register/g)||[]).length,1);
assert.doesNotMatch(pwa,/controllerchange[\s\S]{0,200}location\.reload/);
assert.match(sw,/ARENA_SW_READY/);
const buildId=/BUILD_ID='([^']+)'/.exec(pwa)?.[1];
assert.ok(buildId,'PWA build ID missing');
assert.ok(sw.includes(buildId),'service worker and PWA build IDs differ');
console.log('LGS server-authority security/PWA standard OK');