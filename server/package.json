{
  "name": "arena-edu-server-authority",
  "version": "3.8.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "types": "wrangler types src/worker-configuration.d.ts",
    "deploy:check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.33.1"
  }
}
