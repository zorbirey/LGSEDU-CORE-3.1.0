# Cloudflare free-tier reference skeleton

Deliberately non-deployable until D1/R2 identifiers, authentication, Turnstile, WAF, subscription verification, trusted time, moderation and hard quota/spend limits are configured. No secrets belong here. Protected responses use `Cache-Control: no-store, private`; missing infrastructure fails closed.
