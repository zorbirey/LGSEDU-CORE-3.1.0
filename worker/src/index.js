const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store, private','x-content-type-options':'nosniff'}});
export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/health')return json({standard:'ARENA-SECURITY-CORE-V1',status:'configuration-required'});if(!env.DB||!env.CONTENT)return json({error:'backend_not_configured'},503);if(!request.headers.get('Authorization'))return json({error:'authentication_required'},401);return json({error:'not_implemented'},501)}};
